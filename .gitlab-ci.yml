deploy:
  stage: deploy
  script:
    - sudo bash -l -c 'cd /home/dalphi/services/ner_iterate_python && git pull sudo systemctl stop service-ner_iterate_python.service && sudo systemctl start service-ner_iterate_python.service'
  only:
    - master
